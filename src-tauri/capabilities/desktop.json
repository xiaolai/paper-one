{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "the grants that exist only where their plugins compile: webhost and voices are desktop-only crates, and a platform-less file granting it fails tauri-build's ACL validation on iOS and Android with `Permission webhost:default not found`. Scoped, so the three desktop OSes get it and the phones never ask. (It granted the deleted inference plugin too, which is the pair that made the file worth having.)",
  "platforms": [
    "macOS",
    "windows",
    "linux"
  ],
  "windows": [
    "main"
  ],
  "permissions": [
    "webhost:default",
    "voices:default"
  ]
}
